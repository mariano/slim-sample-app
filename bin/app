#!/usr/bin/env php
<?php
$di = require_once(__DIR__ . '/../src/bootstrap.php');

use Infrastructure\Console\Command\Worker\EmailCommand;
use Symfony\Component\Console\Application;

$application = new Application();
$application->add(new EmailCommand($di->get('queue')));
$application->run();